project: "humemory"
phase: development
progress: 97
last_updated: "2026-07-31"

next_action: "Sprint 7 — S7-02 : solder BUG-05/06/08, puis phase release. Suppression du vanilla au choix de l utilisateur."
next_command: "bmad-sprint-story"
next_role: "architect"
active_role: "scrum"

dependencies: []

phases:
  - name: planning
    status: done
  - name: development
    status: in_progress
  - name: testing
    status: in_progress
  - name: release
    status: upcoming

marketing:
  - "Système mémoire humain-like: dégradation progressive en 5 niveaux"
  - "Recherche inversée: trouve avec indices flous, remonte au détail si match"
  - "Dashboard web 'Palais de Mémoire' avec visualisation consolidation"
  - "API HTTP + CLI + lib importable — intègre dans tout projet"

product:
  - "Core rétrospectif complet : decay 5 niveaux, search inversée, SQLite WAL + advisory lock, CLI, API, dashboard"
  - "Sprints 1+2+3+4+5 livrés. Suite : 180 pass / 0 fail / 390 expect sur 11 fichiers (bun 1.3.14, vérifié 2026-07-31)"
  - "5 visualisations dashboard implémentées (river, galaxy, replay, promenade, decay curve) — B-VIZ-01 clos"
  - "PHASE5_PLAN.md écrit (2026-06-24) — plan Phase 5 corrigé : table intentions dédiée, cues typés, hooks SessionStart + post-commit"
  - "Sprint 5 TERMINÉ (7/7) — Phase 5 complète : mémoire prospective de bout en bout (intentions, cues, resolver, hooks SessionStart + post-commit, CLI, API)"

far_vision:
  - "Mémoire prospective : intentions + cues + hook SessionStart → l'agent reçoit ses boucles ouvertes avant de chercher"
  - "Zeigarnik effect : git commit ferme les loops via marker explicite ou heuristique fichiers"
  - "DB partagée multi-projet avec advisory lock (Phase 5.0)"
  - "Cognitive scripts (Phase 6) : routines pré-chargées sur cue — spec à écrire"

artifacts:
  prd: missing
  architecture: missing
  tech-spec: missing

sprints:
  - id: 1
    goal: "Foundation — core architecture, store, CLI, API, tests"
    status: completed
    stories:
      - id: S1-01
        title: "Core decay algorithm + types"
        status: complete
        tests_executed: true
        test_output: "bun test — 12/12 pass"
        test_result: pass
      - id: S1-02
        title: "SQLite store (migré bun:sqlite)"
        status: complete
        tests_executed: true
        test_output: "bun test — 12/12 pass"
        test_result: pass
      - id: S1-03
        title: "CLI commander (encode/search/recall/list/decay/status/delete)"
        status: complete
        tests_executed: false
        test_output: null
        test_result: null
      - id: S1-04
        title: "API HTTP Hono + dashboard web"
        status: complete
        tests_executed: false
        test_output: null
        test_result: null
      - id: S1-05
        title: "Cron consolidation + session mnésique"
        status: complete
        tests_executed: false
        test_output: null
        test_result: null

  - id: 2
    goal: "LLM integration — auto-génération niveaux, fusion, hook agent"
    status: completed
    stories:
      - id: S2-01
        title: "Auto-génération N1/N2/N3 via LLM (résumé, essentiel, mots-clés)"
        status: complete
        tests_executed: true
        test_output: "bun test — 18/18 pass"
        test_result: pass
      - id: S2-02
        title: "Détection et fusion de souvenirs similaires (niveau 4)"
        status: complete
        tests_executed: true
        test_output: "bun test — 22/22 pass"
        test_result: pass
      - id: S2-03
        title: "Hook agent Claude Code: parse sessions, extraction apprentissages"
        status: complete
        tests_executed: true
        test_output: "bun test — 31/31 pass"
        test_result: pass
      - id: S2-04
        title: "Search enrichie: filtres type/période/associations"
        status: complete
        tests_executed: true
        test_output: "bun test — 34/34 pass"
        test_result: pass
      - id: S2-05
        title: "Photographic mode: désactiver dégradation pour traces critiques"
        status: complete
        tests_executed: true
        test_output: "bun test — 37/37 pass"
        test_result: pass

  # Sprint 3 reconstruit rétroactivement (2026-07-30) depuis l'historique git : le travail
  # avait été livré sans jamais être ouvert dans status.yaml — c'est ce que QA-01 signalait.
  # Sources : ab814f3 (dashboard UX), 6df0cac (visualisations roadmap + fix SEC-01).
  # Note chrono : 6df0cac a atterri après Sprint 4, rattaché ici par cohérence thématique.
  - id: 3
    goal: "Dashboard UX + visualisations roadmap"
    status: completed
    reconstructed_from_git: true
    stories:
      - id: S3-01
        title: "Photographic badge 🔒 + toggle inline par trace (public/index.html)"
        status: complete
        commit: ab814f3
        tests_executed: false
        test_output: null
        test_result: null
      - id: S3-02
        title: "Merge UI : panel similaires (score) + confirmMerge → POST /memories/:id/merge"
        status: complete
        commit: ab814f3
        tests_executed: false
        test_output: null
        test_result: null
      - id: S3-03
        title: "Filtres avancés : dateFrom/dateTo/minSaillance/minRecalls/photo-only + clear"
        status: complete
        commit: ab814f3
        tests_executed: false
        test_output: null
        test_result: null
      - id: S3-04
        title: "Visualisations roadmap (ROADMAP.md options A-E) : river, galaxy, replay, promenade, decay-curve + endpoints API"
        status: complete
        commit: 6df0cac
        tests_executed: false
        test_output: null
        test_result: null
      - id: S3-05
        title: "Fix path traversal SEC-01 : safeJoin() containment sur handlers statiques"
        status: complete
        commit: 6df0cac
        tests_executed: true
        test_output: "build clean; audit AUDIT-2026-06-21 SEC-01 → fixed"
        test_result: pass

  - id: 4
    goal: "Concurrence SQLite + build stability"
    status: completed
    stories:
      - id: S4-01
        title: "SQLite WAL mode + busy_timeout + PRAGMA optimizations"
        status: complete
        tests_executed: true
        test_output: "bun test — 37/37 pass"
        test_result: pass
      - id: S4-02
        title: "Write serialization layer (enqueueWrite promise queue)"
        status: complete
        tests_executed: true
        test_output: "bun test — 37/37 pass"
        test_result: pass
      - id: S4-03
        title: "Fix flaky list test + CLAUDE.md tsc/test doc update"
        status: complete
        tests_executed: true
        test_output: "bun test — 37/37 pass"
        test_result: pass

  - id: 5
    goal: "Mémoire prospective — intentions, cues, hooks SessionStart/post-commit (Phase 5, voir PHASE5_PLAN.md)"
    status: completed
    stories:
      - id: S5-00a
        title: "Phase 5.0 — Advisory lock SQLite cross-process (closes Bug #3)"
        status: complete
        commit: 8118e54
        tests_executed: true
        test_output: "bun test — 40/40 pass, dont tests/advisory-lock-basic.test.ts 3/3 (multi-instance write, recall concurrent sérialisé, cleanup lock file)"
        test_result: pass
      - id: S5-00b
        title: "Phase 5.0 — Event bus injectable dans test env (extension docs/TESTING.md)"
        status: complete
        tests_executed: true
        test_output: "bun test — 58 pass / 0 fail / 123 expect (5 fichiers), dont tests/test-env.test.ts 18/18. Build tsc clean."
        test_result: pass
        delivered:
          - "src/core/clock.ts — Clock, systemClock, FakeClock (advance/advanceHours/advanceDays/set)"
          - "src/core/event-bus.ts — AppEvent typé, EventBus, InMemoryEventBus (dispatch awaité, journal, reset)"
          - "Clock threadé dans SQLiteStore (add/recall/updateDecay) + InverseSearchEngine (bonus de récence)"
          - "Garde assertNotProdDbUnderTest : ouvrir data/humemory.db sous NODE_ENV=test lève"
          - "NoopLock pour DB :memory: — ':memory:.lock' n'est pas un nom de fichier valide sous Windows"
          - "tests/helpers/{store,clock,llm,fixtures}.ts + tests/fixtures/{memories,events}.basic.json"
          - "tests/test-env.test.ts — 4 piliers + e2e decay L0→L1→L2→L3 piloté uniquement par FakeClock"
          - "docs/TESTING.md — pilier 4 event bus, checklist Phase 5 cochée"
        verification: "Test de mutation : rétablir new Date() dans SQLiteStore.add fait tomber 2 tests de test-env — la suite n'est pas vacante."
      - id: S5-01
        title: "Phase 5.1 — Schéma data : tables intentions + cues (migrations idempotentes)"
        status: complete
        tests_executed: true
        test_output: "bun test — 78 pass / 0 fail / 167 expect (6 fichiers), dont tests/intentions.test.ts 20/20. Build tsc clean."
        test_result: pass
        delivered:
          - "src/core/types.ts — Intention, Cue, TriggerSpec (Time|Event), statuts, NewIntention/NewCue, interface IntentionStore"
          - "Tables intentions + cues + 6 index, via CREATE ... IF NOT EXISTS (initProspectiveSchema) — rejouable sur base peuplée"
          - "PRAGMA foreign_keys=ON — sans lui le ON DELETE CASCADE de cues était décoratif"
          - "CRUD : addIntention (+cues dans la même écriture verrouillée), getIntention, listIntentions, updateIntentionStatus, deleteIntention, addCue, getCue, listCues, updateCueStatus"
          - "Horodatages fired_at/closed_at posés par l'horloge injectée ; fired_at non réécrit si déjà posé"
          - "tests/fixtures/loops.open.json + seedIntentions() — deadlines en expiresInHours, jamais absolues"
          - "tests/intentions.test.ts — 20 tests : schéma, CRUD, filtres, transitions, cascade, fixtures"
        verification: "Mutation : retirer PRAGMA foreign_keys=ON laisse 2 cues orphelins et fait tomber le test de cascade. Migration validée sur copie de la vraie data/humemory.db — 5 traces préservées, tables créées, intention armée."
        notes: "EventTriggerSpec n'a volontairement pas de variante 'commit' : un commit ne réveille pas une intention, il la ferme (hook post-commit, S5-03b)."
      - id: S5-02
        title: "Phase 5.2 — Cue resolver : resolveTimeCues + resolveEventCues + expireStale, règles décay × intention (armed/fired/closed)"
        status: complete
        tests_executed: true
        test_output: "bun test — 110 pass / 0 fail / 233 expect (7 fichiers), dont tests/cues.test.ts 32/32. Build tsc clean."
        test_result: pass
        delivered:
          - "src/core/cues.ts — SqliteCueResolver (resolveTimeCues, resolveEventCues, fire, expireStale)"
          - "Matcher cron 5 champs maison (*, n, a-b, */s, listes) — aucune dépendance ajoutée ; expression invalide = jamais due, jamais de crash"
          - "cronDueSince() avec fenêtre de rattrapage de 7 jours : le resolver tourne au SessionStart, pas en démon, une occurrence entre deux sessions serait perdue sans ça"
          - "Scoping par lieu mental : un event d'un autre directory ne réveille rien (DB partagée multi-projet)"
          - "Normalisation des chemins Windows/POSIX + match en suffixe pour file_open"
          - "error_pattern en regex, repli littéral si la regex est invalide"
          - "intentionSaillance() — armed 100 figé, fired décline de 10 pts/jour, closed/expired 0"
          - "store.markCueFired({ rearm }) — un cue cron est ré-armé après tir, sinon la récurrence est un one-shot déguisé"
          - "attachResolverToBus() — branche le resolver sur l'InMemoryEventBus (ou tout EventBus)"
          - "tests/cues.test.ts — 32 tests : cron, matching événementiel, fire, expireStale, règles décay, bout-en-bout via bus"
        verification: "Mutation : neutraliser le scoping par directory fait tomber le test de fuite inter-projets."
        notes: "FileSystemEventBus toujours absent — les events viennent des hooks (S5-03a/b), pas d'un watcher. Décision reconduite depuis S5-00b."
      - id: S5-03a
        title: "Phase 5.3 — Hook scripts/hook-session-start.ts : résout cues du cwd/branch, markdown sur stdout, budget HUMEMORY_SESSION_BUDGET"
        status: complete
        tests_executed: true
        test_output: "bun test — 128 pass / 0 fail / 274 expect (8 fichiers), dont tests/session-context.test.ts 18/18. Build tsc clean. Script éprouvé bout-en-bout sur DB temporaire."
        test_result: pass
        delivered:
          - "src/agent/session-context.ts — buildSessionContext() : ménage (expireStale + tir des cues échus) puis composition markdown"
          - "scripts/hook-session-start.ts — coquille fine : lit env + branche git, écrit sur stdout, ne bloque jamais la session (exit 0 même en erreur)"
          - "Sections : ⏰ Échéances atteintes, Boucles ouvertes (Zeigarnik), Traces pertinentes dégradées"
          - "Scoping par directory : les boucles des autres projets ne fuitent pas dans le contexte"
          - "Traces rappelées = niveaux 2-3 ET saillance >= seuil, rendues au niveau où elles sont tombées"
          - "loopId()/extractLoopIds()/matchIntentionByShortId() — identité courte 'loop-a1b2c3d4', préfixe ambigu = rien fermé (préparé pour S5-03b)"
          - "store.list() gagne les filtres directory, levels[], minSaillance"
          - "Env : HUMEMORY_DB, HUMEMORY_DIR, HUMEMORY_SESSION_BUDGET, HUMEMORY_SAILLANCE_MIN, HUMEMORY_VERBOSE — documentés dans README"
          - "tests/session-context.test.ts — 18 tests"
        verification: "Exécution réelle du script : branche git détectée, boucle d'un autre projet filtrée, markdown propre sur stdout. Modes dégradés vérifiés — DB inaccessible et budget invalide sortent en exit 0 sans rien écrire sur stdout."
        notes: "Bloc vide si rien de pertinent — on n'injecte pas de section creuse dans le prompt. Sans resolver fourni, la composition est en lecture seule (aucun effet de bord)."
      - id: S5-03b
        title: "Phase 5.3 — Hook .githooks/post-commit : ferme intentions via 'Closes loop-<id>' + heuristique fichiers"
        status: complete
        tests_executed: true
        test_output: "bun test — 180 pass / 0 fail / 390 expect (11 fichiers), dont tests/commit-closer.test.ts 20/20. Build tsc clean. Hook joué dans un vrai dépôt git jetable."
        test_result: pass
        delivered:
          - "src/agent/commit-closer.ts — applyCommitToLoops() + renderCommitReport(), logique séparée de git pour rester testable"
          - "Fermeture explicite 'Closes loop-<id>' : ferme, enregistre le SHA, annule les cues restants"
          - "Heuristique de recoupement fichiers × contenu : suggère uniquement, ne ferme JAMAIS d'office"
          - "Tokenisation avec stopwords (src, index, test…) — sans ça tout recoupe tout"
          - "scripts/hook-post-commit.ts — interroge git, affiche, exit 0 systématique (le commit est déjà écrit)"
          - ".githooks/post-commit — shim sh, activé par git config core.hooksPath .githooks"
          - "tests/commit-closer.test.ts — 20 tests, aucun appel git (CommitInfo injecté)"
        verification: "Dépôt git jetable : commit sans marqueur → suggestion affichée, boucle inchangée ; commit 'Closes loop-xxx' → fermée, cue annulé, SHA enregistré, autre boucle intacte. Shim vérifié via core.hooksPath sur un dépôt tiers."
        fixes:
          - "Le shim résolvait le script depuis la racine du dépôt commité — il supposait que humemory EST ce dépôt. Inutilisable dans un autre projet alors que la base est justement partagée. Corrigé via HUMEMORY_HOME + garde [ -f ] avant exécution."
        notes: "Écart assumé au plan : PHASE5_PLAN parlait de scorer contre les 'keywords de l'intention', mais une intention n'a pas de keywords — le score porte sur les jetons du contenu. Les ids non hexadécimaux ne sont pas des références de boucle (les ids sont des préfixes d'UUID)."
      - id: S5-04
        title: "Phase 5.4 — CLI 'intent {add,list,close,fire}' + API POST /intentions, POST /cues, POST /events"
        status: complete
        tests_executed: true
        test_output: "bun test — 160 pass / 0 fail / 353 expect (10 fichiers), dont api-intentions 22/22 et cue-arg 10/10. Build tsc clean. CLI exercée bout-en-bout sur DB temporaire."
        test_result: pass
        delivered:
          - "src/api/intentions-routes.ts — sous-routeur Hono recevant son store en paramètre (donc testable), monté par server.ts"
          - "Routes : POST/GET /intentions, GET /intentions/:id (+cues), POST /intentions/:id/{close,fire}, DELETE /intentions/:id, POST/GET /cues, POST /events, POST /cues/resolve"
          - "Validation stricte des entrées réseau (validateTriggerSpec / validateAppEvent) — 400 plutôt que stocker un cue qui ne réveillera jamais rien"
          - "src/cli/cue-arg.ts — parseCueArg/formatTriggerSpec, aller-retour testé ; gère les ':' dans les regex et les chemins Windows"
          - "CLI 'intent add|list|close|fire|resolve' — ids courts loop-xxxxxxxx acceptés, préfixe ambigu refusé (exit 1)"
          - "CLI respecte HUMEMORY_DB (même convention que les hooks) — permet de l'exercer sans toucher la prod"
          - "server.ts : serve() conditionné à import.meta.main + export de app — importer le module ne lève plus de serveur"
          - "tests/api-intentions.test.ts 22 tests + tests/cue-arg.test.ts 10 tests"
        verification: "CLI jouée en réel sur DB temporaire : add (2 cues + échéance) → resolve (échéance tirée) → close --commit → list. Id inconnu refusé avec exit 1."
        fixes:
          - "Chemins relatifs : le lieu mental est désormais résolu en absolu (encode, search, intent add/list, import-session). Une boucle armée sur './src/auth' n'aurait jamais matché le hook SessionStart qui filtre par process.cwd() — échec silencieux."
        notes: "Les routes historiques de server.ts (S1-04) restent non testées : le store y est créé au chargement du module. Le sous-routeur prospectif contourne le problème sans réindenter 390 lignes ; refactor de server.ts non fait, tracé ci-dessous."

  - id: 6
    goal: "Front React — socle (bundler bun + @testing-library) puis onglet prospectif, portage progressif des vues"
    status: in_progress
    rationale: "Demande explicite (lisibilité recrutement). Bundler bun retenu plutôt que Vite : cohérent avec le runtime, zéro dépendance de build supplémentaire."
    stories:
      - id: S6-01
        title: "Socle : bundler bun + React + TS + @testing-library, servi par Hono, types partagés avec le back"
        status: complete
        tests_executed: true
        test_output: "bun test — 187 pass / 0 fail / 403 expect (12 fichiers), dont tests/web-app.test.tsx 7/7. typecheck:web clean, build complet clean."
        test_result: pass
        delivered:
          - "web/ — App.tsx, main.tsx, index.html, styles.css, api/client.ts, hooks/useAsync.ts, components/LoopBadge.tsx"
          - "Bundler natif bun (bun build sur entrée HTML) — aucune dépendance de build ajoutée, 189 Ko en production"
          - "web/tsconfig.json séparé (jsx, DOM) — le tsconfig racine ne compile que src/, le front ne peut pas casser le build de la lib"
          - "Types partagés : web/api/client.ts importe Intention/Cue/Memory depuis src/core/types.ts — zéro type dupliqué"
          - "Routes Hono /app et /app/* avec safeJoin (garde SEC-01 réutilisée) ; 503 explicite si le bundle manque"
          - "bunfig.toml preload tests/setup-dom.ts (happy-dom) — @testing-library tourne sous bun test, un seul runner"
          - "Scripts : build:web, dev:web (watch), typecheck:web ; pnpm build enchaîne tsc puis le bundle"
          - "useAsync : ignore les réponses périmées et les setState après démontage"
          - "public/app/ gitignoré — artefact de build"
        verification: "API lancée sur base de démo : /app sert le HTML, les assets hashés sortent avec le bon Content-Type, /app/../../package.json → 404 (traversée bloquée), /intentions renvoie les 2 boucles armées. Les 180 tests backend restent verts avec le DOM préchargé."
        fixes:
          - "server.ts ignorait HUMEMORY_DB (contrairement à la CLI et aux hooks) — impossible de lancer l'API sur une base de démo sans écrire dans la production. Corrigé."
        notes: "Front vanilla conservé à / pendant le portage. Pas de capture navigateur : l'extension Chrome n'était pas connectée ; le rendu est couvert par les tests de composants et le service des assets vérifié en HTTP."
      - id: S6-02
        title: "Onglet prospectif : boucles ouvertes (tension/saillance), cues, fermeture — consomme /intentions"
        status: complete
        tests_executed: true
        test_output: "bun test — 200 pass / 0 fail / 425 expect (13 fichiers), dont web-loops 13/13 et web-app 7/7. typecheck:web clean, build clean."
        test_result: pass
        delivered:
          - "web/components/LoopTension.tsx — barre de tension alimentee par intentionSaillance, la fonction du back : la regle n'est pas reimplementee cote front"
          - "web/components/LoopCard.tsx — identifiant court, tension, lieu, echeance, cues avec leur statut, actions"
          - "web/components/NewLoopForm.tsx — armement, cues au format CLI valides avant envoi"
          - "web/api/hydrate.ts — DTO ISO vers vraies Date, type IntentionView (modele + loopId d'affichage)"
          - "App : filtres par statut, filtre par lieu mental, bouton de balai, notices"
          - "src/cli/cue-arg.ts deplace vers src/core/cue-arg.ts — meme parseur pour la CLI et le formulaire"
          - "tests/web-loops.test.tsx — 13 tests (tension, carte, formulaire)"
        verification: "Cycle complet contre l'API reelle : POST /intentions (201, cue arme) puis detail puis close (cue passe cancelled) puis filtres armed=0 closed=1."
        design:
          - "Suppression a deux temps : confirmation obligatoire, jamais en un clic"
          - "Reveiller desactive si aucun cue arme"
          - "Une boucle fermee n'offre plus d'action"
          - "L'instant de reference est calcule une fois par rendu : deux boucles cote a cote sont evaluees au meme instant"
        notes: "Les cues sont charges par un appel de detail par boucle : l'API n'a pas de route de liste jointe, en inventer une pour l'ecran aurait ete premature. A revoir si la liste grossit."
      - id: S6-04
        title: "Parité : Replay, fusion/similaires, encodage manuel, filtres avancés"
        status: complete
        tests_executed: true
        test_output: "bun test — 238 pass / 0 fail / 963 expect (15 fichiers), dont web-replay-merge 11/11 et web-traces 23/23. typecheck:web clean, build splitté clean."
        test_result: pass
        delivered:
          - "web/components/ReplayTab.tsx — rejeu de session en JSX : curseur, lecture, compteurs déduits de l'index"
          - "web/components/SimilarPanel.tsx — similaires + fusion, confirmation inline montrant la cible (l'original utilisait confirm() qui bloque l'onglet)"
          - "web/components/NewTraceForm.tsx — encodage manuel (contenu, lieu, type, mots-clés, photographique)"
          - "Filtres avancés dans TracesTab : dates, force min, réactivations min, lieu mental ; bascule automatique vers /search quand un filtre est actif"
          - "tests/web-replay-merge.test.tsx 11 tests + 6 tests ajoutés à web-traces"
        verification: "Contre l'API réelle : GET /sessions et /sessions/:id renvoient la session et ses 2 events ; GET similar trouve 1 candidat ; POST merge passe la source au niveau 4 avec mergedIntoId pointant la cible."
        notes: "Le sélecteur de contexte du vanilla (liste déroulante des répertoires) devient un champ libre 'lieu mental' — moins restrictif, même usage. Parité atteinte : le dashboard vanilla peut être retiré, décision laissée à l'utilisateur, rien côté React n'en dépend."

      - id: S6-03
        title: "Portage des vues existantes ; three.js et canvas wrappés dans useEffect, jamais réécrits"
        status: complete
        tests_executed: true
        test_output: "bun test — 221 pass / 0 fail / 919 expect (14 fichiers), dont web-traces 17/17. typecheck:web clean, build splitté clean."
        test_result: pass
        delivered:
          - "web/components/ImperativeView.tsx — hote generique d'une vue impérative, import dynamique + démontage garanti"
          - "web/viz/{river,galaxy,promenade,decay-curve}.ts — modules ESM convertis depuis public/js, tracés repris tels quels"
          - "web/components/TracesTab.tsx + zones.ts — seul portage JSX (dashboard.js était fait d'innerHTML)"
          - "Shell à 5 onglets ; cliquer une trace dans une viz renvoie vers l'onglet Traces"
          - "projectDecayCurve ajouté à src/core/decay.ts (+4 tests) — le front ne réimplémente plus l'algorithme"
          - "d3 et @types/three ajoutés ; les CDN d3js.org et jsdelivr disparaissent du chemin React"
          - "Code splitting : entrée 204 Ko, three.js isolé dans un chunk de 0,54 Mo chargé à l'ouverture de l'onglet"
          - "tests/web-traces.test.tsx — 17 tests (zones, contrat de montage/démontage, onglet Traces)"
        fixes:
          - "public/js/decay.js dupliquait calculateDecayLevel/calculateSaillance/calculateDecayRate du back : la duplication disparaît du chemin React"
          - "promenade.js attachait 5 écouteurs à document/window jamais retirés — ZQSD continuait de piloter une caméra invisible après changement d'onglet. AbortController + dispose des géométries/matériaux/textures"
          - "river.js laissait tourner son setInterval et abandonnait sa tooltip dans le body au démontage"
          - "Hauteur des sphères de la promenade : Math.random() remplacé par une dérivation de l'index — le palais doit être le même d'une visite à l'autre"
        remaining: "Replay, fusion, encodage et filtres avancés livrés depuis en S6-04 — la parité est atteinte."
        notes: "4 vues sur 5 sont wrappées et non réécrites : river et galaxy sont pilotées par d3, qui possède son sous-arbre SVG. Seul dashboard.js, fait de chaînes innerHTML, méritait un portage JSX."

  - id: 7
    goal: "Bascule du front React à la racine + dette tracée avant release"
    status: in_progress
    stories:
      - id: S7-01
        title: "Servir l'app React à / , conserver le vanilla sur /legacy"
        status: complete
        tests_executed: true
        test_output: "bun test — 238 pass / 0 fail / 964 expect (15 fichiers). Build clean. Routes vérifiées en HTTP réel."
        test_result: pass
        delivered:
          - "GET / sert l'app React ; repli sur le dashboard vanilla si le bundle est absent"
          - "GET /legacy conserve l'ancien dashboard — filet de sécurité, rien n'en dépend"
          - "GET /app conservé (URL historique)"
        fixes:
          - "Les assets étaient référencés en relatif (./index-abc.js) : servie à /app sans slash final, la page les cherchait à la racine et ne les trouvait pas. L'app React était donc cassée en navigateur réel, ce que mes vérifications HTTP n'avaient pas montré (elles tapaient les assets par leur chemin direct). Les références sont désormais absolutisées vers /app/."
        verification: "Les trois routes servent des assets qui répondent 200 depuis l'URL telle qu'écrite dans la page ; les 5 chunks dynamiques (dont three.js) sont joignables ; #root présent."
        notes: "Suppression du vanilla non faite : c'est une suppression, elle reste à la main de l'utilisateur."
      - id: S7-02
        title: "Dette : BUG-05 (suites héritées vers fixtures), BUG-06 (vitest orphelin), BUG-08 (createApp testable)"
        status: pending
        tests_executed: false
        test_output: null
        test_result: null

backlog:
  - id: B-VIZ-01
    title: "Roadmap visualisation (ROADMAP.md options A-E) — 100% implémentée (river/galaxy/replay/promenade/decay-curve)"
    priority: low
    status: done
    resolution: "Vérifié 2026-07-30 : les 5 modules existent dans public/js/, 21 refs dans index.html. Tracké comme S3-04. Reste à marquer ROADMAP.md done."
  - id: B-PHASE6
    title: "Phase 6 — Cognitive scripts : écrire spec avant implémentation (template vs intentions chaînées vs prompt vs tool bundle)"
    priority: low
    status: open
  - id: B-DOC-01
    title: "Vérifier date 2026-05-03 dans config.yaml (date suspecte / future)"
    priority: low
    status: done
    resolution: "Fausse alerte — 2026-05-03 = date du commit initial 8a92ac0. Date légitime, aucun changement."

qa:
  bugs:
    - id: BUG-03
      title: "SQLite multi-process : pas de cross-process advisory lock (WAL + write-queue OK intra-process seulement)"
      severity: critical
      impact: "Bloquant Phase 5 — cue resolver background + hooks + dashboard écrivent en concurrence"
      status: fixed
      target_story: S5-00a
      fix: "AdvisoryLock class avec file-based lock (wx exclusive creation) + enqueueWriteWithLock pour combiner intra-process queue + cross-process lock"
    - id: BUG-04
      title: "tsc global peut shadow le tsc local — toujours utiliser pnpm build (= tsc -p tsconfig.json)"
      severity: minor
      status: known
    - id: BUG-05
      title: "Suites héritées seedent des littéraux ad hoc au lieu de tests/fixtures/"
      severity: minor
      impact: "Viole docs/TESTING.md + CLAUDE.md ('Seed memories from tests/fixtures/, not ad hoc literals')."
      status: partial
      target_story: S5-00b
      fix: "S5-00b a créé tests/fixtures/ + tests/helpers/ et les utilise dans test-env.test.ts. Sévérité abaissée major → minor : la fondation existe et n'est plus bloquante pour Phase 5."
      remaining: "Migrer les 3 suites héritées (humemory.test.ts, agent.test.ts, llm-generator.test.ts) vers freshStore()/seedMemories() — elles passent mais restent sur DB fichier + littéraux inline."
    - id: BUG-08
      title: "server.ts crée son store au chargement du module — routes historiques (S1-04) non testables"
      severity: minor
      impact: "Les routes /memories, /search, /status, /sessions n'ont aucun test. Importer server.ts sous NODE_ENV=test lèverait via le garde prod-DB."
      status: open
      fix_suggested: "Extraire createApp(store) comme pour intentions-routes.ts ; réindentation de ~390 lignes, à faire hors story."
    - id: BUG-06
      title: "vitest.config.ts orphelin — toute la suite tourne sous bun test"
      severity: minor
      impact: "Runner ambigu ; un contributeur peut lancer vitest et croire la suite vide."
      status: open
    - id: BUG-07
      title: "Artefact de test orphelin tests/test-advisory-lock.db (32 Ko) laissé sur disque"
      severity: minor
      impact: "Viole la contrainte hermétique. Résidu d'un test supprimé — aucune suite ne le référençait."
      status: fixed
      fix: "Fichier supprimé le 2026-07-30. advisory-lock-basic.test.ts utilise test-advisory-lock-basic.db et nettoie en beforeEach/afterEach — mais ne purge pas les sidecars -wal/-shm (à traiter en S5-00b)."
  audits:
    - id: AUDIT-2026-06-21
      role: reviewer
      health_score: 7
      scope: "Uncommitted roadmap work — dashboard refactor (index.html -1185 LOC) + new API endpoints (server.ts +110)"
      findings:
        - severity: critical
          id: SEC-01
          title: "Path traversal in /css/*, /js/*, /assets/* static handlers (CWE-22)"
          detail: "filePath taken from c.req.path with no containment; ../ escaped PUBLIC_DIR → arbitrary file read"
          status: fixed
          fix: "Added safeJoin() resolve-based containment guard in src/api/server.ts; build clean"
        - severity: minor
          id: QA-01
          title: "Sprint 3 absent from status.yaml (jumps S2→S4); S1-03/04/05 tests_executed:false"
          status: fixed
          fix: "Sprint 3 reconstruit depuis git (ab814f3, 6df0cac) en 5 stories S3-01..S3-05, 2026-07-30. S1-03/04/05 restent tests_executed:false — CLI/API/cron sans couverture directe, reporté (voir BUG-05)."
        - severity: minor
          id: QA-02
          title: "Test suite was unverified — bun not installed. Installed bun 1.3.14; ran suite: 37 pass / 0 fail / 81 expect (3 files). Claim confirmed."
          status: fixed
      verified:
        - "SQL fully parameterized (sqlite.ts dynamic WHERE uses static conditions + bound params) — no injection"
        - "CORS locked to localhost dev / CORS_ORIGINS prod; credentials gated"
        - "/sessions/:id filters in-memory only, no file read"
    - id: AUDIT-2026-07-30
      role: scrum
      scope: "Audit de synchronisation bmad ↔ code réel (demandé par l'utilisateur : 'vérifie si le bmad est synchro / legit')"
      verdict: "Légit sur le fond, désynchro sur la forme — aucune story marquée complete sans code derrière."
      verified:
        - "S5-00a réel : class AdvisoryLock src/store/sqlite.ts:15, enqueueWriteWithLock câblé sur 7 chemins d'écriture"
        - "Phase 5 à zéro ligne : aucune occurrence de intention/cue/resolveTimeCues dans src/ ni scripts/ — les 6 stories pending sont honnêtes"
        - "B-VIZ-01 exact : les 5 modules de visualisation existent dans public/js/"
        - "Sprints 1/2/4 : code présent pour chaque story"
        - "config.yaml created 2026-05-03 == date du commit initial 8a92ac0"
      corrections:
        - "Compte de tests 37/37 → 40 pass / 88 expect / 4 fichiers (advisory-lock-basic.test.ts jamais reporté)"
        - "status.md périmé (affichait S5-00a ⬚ et Sprint 5 0/7, réel 1/7) → régénéré"
        - "QA-01 clos : Sprint 3 reconstruit depuis git"
        - "B-DOC-01 clos : fausse alerte"
        - "BUG-05/06/07 ouverts : écarts constatés hors périmètre bmad (fixtures, vitest orphelin, artefact de test)"
