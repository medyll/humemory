{
  "$comment": "Open loops (Zeigarnik effect) for prospective memory. Dates are relative to T0 through expiresInHours — never an absolute timestamp. Cues are declared inline and armed together with their intention.",
  "intentions": [
    {
      "key": "refactor-auth",
      "content": "Refactor the token validation in the auth middleware — the off-by-one is fixed but the function is still unreadable.",
      "directory": "/src/auth",
      "expiresInHours": 168,
      "cues": [
        { "kind": "event", "type": "file_open", "path": "src/auth/service.ts" }
      ]
    },
    {
      "key": "bench-decay",
      "content": "Measure the cost of updateDecay() over 10k traces before opening Phase 6.",
      "directory": "/src/core",
      "expiresInHours": 720,
      "cues": [{ "kind": "time", "at": "2026-01-08T09:00:00.000Z" }]
    },
    {
      "key": "doc-prospective",
      "content": "Document the intentions/cues model in AGENTS.md once the resolver ships.",
      "directory": "/docs",
      "cues": [
        { "kind": "event", "type": "branch_switch", "branch": "feature/prospective-memory" },
        { "kind": "time", "cron": "0 9 * * 1" }
      ]
    },
    {
      "key": "expired-loop",
      "content": "Already overdue loop: exercises expireStale() without hand-crafting a date.",
      "directory": "/src/core",
      "expiresInHours": 1,
      "cues": [{ "kind": "time", "at": "2026-01-01T00:30:00.000Z" }]
    }
  ]
}
